## 0.0.1

* Initial release.
* Add `ProjectUtils.getPlatformVersion()` for Android, iOS, Web, Windows, macOS, and Linux.
* Add `ToastUtils` with text, success, error, info, and loading toasts.
* Add `ToastUtils.appBuilder()` and `ToastUtils.navigatorObservers()` for BotToast setup.
* Add `KeepAliveWrapper` for preserving child state in scrollable tabs.
